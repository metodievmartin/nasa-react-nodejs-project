# API URL Configuration
# In development: Use the full URL to the backend server
# In production: Use just the path (/api/v1)
# 
# Note: Create React App requires environment variables to start with REACT_APP_
# to be accessible in the JavaScript code
REACT_APP_API_URL=http://localhost:8000/api/v1
